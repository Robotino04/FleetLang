let main = () -> i32 {
    exit(5);
    exit(5)
    exit(5);
    exit(5);
    exit(5);
    exit(5);
    exit(5);
    exit(5)
    exit(5);
    exit(5);
}

let main

let main = () -> i32 {
    exit(5);
}

let main = 

let main = () -> i32 {
    exit(5);
}

let main = () -> i32 {
    exit(5);
}

let main = () -> i32 {
    exit(5);
}


let main = () ->

let main = () -

let main = () -> i32 {
    exit(5);
}

&&&&
&
      &//?

let main = () -> i32 {
    exit(5);
}

